"""Provider-specific tick normalization lives in each source module.

This file exists as a placeholder for shared normalization helpers that may
emerge as more sources are added (e.g., a common timestamp parser, symbol
mapping table, etc.).
"""
